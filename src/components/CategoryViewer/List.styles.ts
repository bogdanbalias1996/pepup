import { StyleSheet } from 'react-native';

import {
  colorLightGray,
  boldFont,
  semiboldFont
} from '../../variables';

export default StyleSheet.create({
  row: {
    flex: 1
  },
  celebsWrapper: {
    flex: 1,
    justifyContent: 'center'
  }
});
