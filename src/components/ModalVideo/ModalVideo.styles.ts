import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  wrapVideo: {
    position: 'absolute',
    width: '100%',
    height: 300,
    alignItems: 'center',
    justifyContent: 'center',
    zIndex: 30
  }
});
